# Resend 통합 설치 후 `vercel env pull .env.local --yes` 로 자동 주입됨
RESEND_API_KEY=

# 상담 신청 메일을 받을 주소 (Vercel 대시보드 환경변수에도 동일하게 등록 필요)
# 주의: Resend 도메인 인증 전에는 Resend 가입 계정 이메일로만 수신 가능
CONTACT_TO_EMAIL=hoon_tax@naver.com

# (선택) 도메인 인증 후 발신 주소. 미설정 시 onboarding@resend.dev 사용
CONTACT_FROM_EMAIL=
